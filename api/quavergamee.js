export default function handler(req, res) { const userAgent = req.headers['user-agent'] || ''; if (userAgent.includes("Discordbot/")) { res.writeHead(302, { Location: 'https://i.postimg.cc/SKFXZj9K/quaver.gif' }); } else { res.writeHead(302, { Location: 'quaver:/' }); } res.end(); }
